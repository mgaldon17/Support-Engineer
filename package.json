{
  "name": "support-engineer-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling (ESLint) for the Support Engineer control-panel front-end. The dashboard itself ships no JS dependencies and runs as plain ES modules.",
  "scripts": {
    "lint:js": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
